"""The hiring pipeline: openings, applications and recorded moves (week 3).

Import from the modules directly (pipeline.store, pipeline.lists), so `python -m pipeline.lists`
runs without importing itself twice.
"""
