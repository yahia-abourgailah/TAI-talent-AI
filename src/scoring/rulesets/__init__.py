"""One module per criteria version, named by ratification date.

A published version is never edited: a rule change adds a new module, and every evaluation
records the version that produced it (BR-303, BR-307).
"""
